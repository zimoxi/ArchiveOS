# START_HERE

你现在维护的是唯一项目：ArchiveOS / OpenFDAMS。

后续所有 Delivery 都是增量补丁，不是新项目。

## 禁止

- 不要新建第二个项目
- 不要覆盖整个项目
- 不要删除已有代码
- 不要自行修改架构
- 不要跳过 NEXT_ACTION
- 不要提前开发未来 Phase

## 正确流程

读取 START_HERE → 读取 PROJECT_PRINCIPLES → 读取 CURRENT_STATUS → 读取 NEXT_ACTION → 执行指定任务 → Git Commit。
